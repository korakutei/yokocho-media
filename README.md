# ヨコチョナビ（Next.js MVP）

「横丁メディア構想」プロトタイプのNext.js実装。Cowork上のArtifact版
（黒×白×金・和と新のデザイン）を、そのままNext.js（App Router / TypeScript）に
移植したものです。デザイン・構成・コピーはArtifact版から意図的に変更していません。

**2026年9月5日、サービス名を「横丁メディア」から「ヨコチョナビ」に変更しました**
（コピーライター3名によるネーミング検討の結果。「メディア」という語尾が
記事配信に閉じた印象を与え、目指す「各横丁への入口」という立ち位置と
ズレていたため）。リポジトリ名・npmパッケージ名(`yokocho-media`)は
変更コストを避けるため据え置いています。

## セットアップ

```bash
npm install
npm run dev
# http://localhost:3000
```

`npm run dev` は `-H 0.0.0.0` 付きで起動するため、同一LAN内であれば
`http://<このPCのIPアドレス>:3000`（例: `http://172.16.80.228:3000`）でも
確認できます（同じPC上で `npm run dev` を実行している場合、社内ブラウザから
直接開けます）。

本番ビルド：

```bash
npm run build
npm run start
```

Vercel等にそのままデプロイ可能な構成です（next.config.jsは最小限）。

**注意:** `app/layout.tsx` の `metadata.robots` を `{ index: false, follow: false }` に
設定し、検索エンジンにインデックスされないようにしています（クローズド運用中のため）。
一般公開の判断が出たら、この設定を外すこと。

## ディレクトリ構成

```
app/
  layout.tsx      … 全体レイアウト、フォント読み込み、メタ情報
  page.tsx         … トップページ（各セクションを組み立てるだけ）
  globals.css      … Artifact版から移植したデザインシステム一式
  venues/[slug]/page.tsx … 横丁詳細ページ（下記「読む・探す・つながるの実装状況」参照）
components/
  Hero.tsx         … ファーストビュー（写真・ワードマーク・日本地図・件数）
  JapanMap.tsx     … Hero内の日本地図イラスト（画像埋め込み。下記「日本地図イラストについて」参照）
  Pillars.tsx      … 「読む・探す・つながる」3本柱
  Digest.tsx       … 「本日の横丁ニュース」一覧（data/digest.jsonを描画）
  Venues.tsx       … 掲載横丁一覧（data/venues.jsonを描画。licensed/research混在）
  UranoProfile.tsx … 横丁交流師・浦野雄次氏のプロフィール欄
  SiteFooter.tsx   … フッター
  Reveal.tsx       … スクロール時のフェードイン演出（クライアントコンポーネント）
lib/
  types.ts         … DigestItem / Venue などの型定義
  data.ts          … data/*.json の読み込み関数
data/
  digest.json      … 当日の横丁ニュース一覧（運用で書き換える対象）
  venues.json      … 掲載横丁一覧（浜倉的商店製作所グループ+全国リサーチ横丁、計22件）
scripts/
  lib/digest-io.ts   … digest.json のバリデーション・読み書き共通ロジック（JST日付計算を含む）
  update-digest.ts   … digest.json の手動更新スクリプト（下記参照）
  collect-digest.ts  … Claude API（web_search）による日次自動収集スクリプト（下記参照）
.github/workflows/
  digest-daily.yml … 毎朝7時（JST）に collect-digest.ts を実行するGitHub Actions
public/images/
  hero-cover.jpg       … Hero背景（未許諾の汎用イメージカット、下記参照）
  hero-shibuya.jpg     … 旧Hero背景（渋谷横丁の公式サイト掲載画像、掲載許諾済み。
                          現在は未使用だが venues/shibuya の詳細ページ用素材とは別物として残置）
  pillar-*.jpg         … 3本柱カードのイメージカット（下記参照、未許諾の汎用素材）
  connect/circle-*.jpg … Hero地図両サイドの円形イメージ写真（下記参照、未許諾の汎用素材）
  japan-map-illustration.png … Hero内の日本地図イラスト（下記参照、未許諾の汎用イメージ）
  urano-yuji.jpg       … 交流師・浦野雄次氏のプロフィール写真（本人提供）
  venues/*.jpg         … 各横丁の公式ロゴ・写真（浜倉的商店製作所グループ公式サイトより掲載許諾済み）
```

## 日本地図イラストについて

Hero下部の「全国各地に、横丁の灯りがある」の地図（`components/JapanMap.tsx`）は、
2026-09-06以降、`public/images/japan-map-illustration.png`（デザイン提供の
未許諾イメージイラスト、`SiteFooter.tsx`の「汎用イメージカット」に含めて表記）を
そのまま表示する方式に変更した。

**変更前の経緯**：当初はWikimedia Commons「Blank map of Japan new.svg」
（CC BY-SA 3.0）の輪郭パスを抽出したSVGを自作し、13地域
（札幌・仙台・東京・千葉・神奈川・名古屋・京都・大阪・岡山・広島・福岡・鹿児島・沖縄）
のピン座標を目視較正していたが、実際の陸地形状とのズレ（特に鹿児島・大阪・京都周辺）が
繰り返し発生し、座標計算による較正では実用上安定しなかった。ユーザーから提供された
「あらかじめ正しい位置に光の点が配置されたイラスト画像」を採用することで、この問題を
根本的に解消した（CC BY-SA由来のSVGパスデータは削除済み、`SiteFooter.tsx`の
Wikimedia表示義務クレジットも撤去済み）。

ピンは雰囲気演出であり、正確な店舗所在地を示す機能地図ではない。画像差し替えの際は
`public/images/japan-map-illustration.png`を置き換え、`components/JapanMap.tsx`の
`width`/`height`（Next.js Imageの必須プロパティ）を新しい画像の実寸に合わせて更新すること。

## 写真について

写真の由来が2系統ある点に注意。

**1. 浜倉的商店製作所グループから掲載許諾済みの素材**（`venues/*.jpg`）

企業から掲載許諾を得た上で、公式サイト（https://www.hamakura-style.com/ ）の
各横丁ページから取得した素材。

- Venuesセクションの各カードは `/venues/[slug]` の横丁詳細ページへの内部リンク。
  詳細ページ側で `photo`（代表写真）+ `gallery`（追加写真2枚程度）+ `logo`
  （毛筆ロゴ）+ `url`（運営元公式サイトへの外部リンク、副次的な導線として設置）
  を表示する。カード→詳細ページ→(必要なら)公式サイト、という順にすることで、
  「渋谷横丁 営業時間」等の検索流入をヨコチョナビ自身が受け止められるようにしている
  （マーケティングコンサルタント・Webデザイナーによるレビューでの指摘を反映、
  2026-09-05）
- 各横丁ページの写真は `<img>` タグではなくCSS `background-image` の
  カルーセルで実装されていることが多く、単純なHTML取得では見つからない
  （実際に取得する際はレンダリングして`computedStyle`を確認する必要があった）
- **ピット・インのみ、公式サイト上に該当ページ・ロゴ・写真が見当たらず未確認。**
  浜倉的商店製作所グループの現行ブランドかどうか含め要確認（掲載対象から
  外すか、別途確認が必要）。`photo`/`logo`/`url` すべて `null` にしている
- **立ち寿司横丁は都内に複数店舗を展開するブランドであり、単一の「横丁」施設
  ではない点にも留意**（venues.jsonの `area` が未確定なのはこのため）

**2. 未許諾の汎用イメージカット**（`hero-cover.jpg`, `pillar-*.jpg`, `connect/circle-*.jpg`）

Hero背景、Pillars（読む・探す・つながる）カードの3枚、Hero地図両サイドの
円形写真5枚は、実在の横丁を指すものではない雰囲気カットで、掲載許諾は
取得していない。それぞれ「イメージ」ラベルを表示している（`Hero.tsx` の
`.hero-photo-tag`、`Pillars.tsx` / `globals.css` の `.pillar-photo-tag`。
円形写真は装飾要素のため個別タグはなし、`alt=""` で装飾画像として扱っている）。
実店舗写真に差し替える際は、`public/images/` の該当ファイルを置き換え、
`Pillars.tsx` の `alt` テキストも実際の状況に合わせて更新すること。

**3. 全国リサーチによる試験掲載横丁**（`venues.json` の `status: "research"`）

浜倉的商店製作所グループ以外の12地域（東京を除く）は、運営者への掲載許諾を
取得しておらず、写真・ロゴは掲載していない（`photo`/`logo` は `null`）。
Venuesカードは各運営者の公式サイト・出典ページへの外部リンクのみで構成され、
「試験掲載」であることを明記している。掲載を望まれない場合は取り下げる方針。

## 日次ニュース更新の運用について

`data/digest.json` の更新には2つの経路がある。

### 1. 自動収集（本運用）

`.github/workflows/digest-daily.yml` が毎朝7時（JST）に起動し、
`npm run digest:collect`（`scripts/collect-digest.ts`）を実行する。このスクリプトは
Claude（`claude-sonnet-5`）にサーバーサイドの `web_search` ツールを渡して直近の
横丁関連ニュースを収集・要約・確信度判定させ、構造化ツール呼び出し
（`submit_digest`）として結果を受け取って `data/digest.json` を書き換える。
掲載基準を満たす新着が0件のときは既存内容を維持する（Artifact版と同じルール）。

**セットアップに必要な作業（このリポジトリのコードでは自動化していない部分）**

1. GitHubリポジトリを作成し、このリポジトリをpushする。
2. リポジトリの Secrets に `ANTHROPIC_API_KEY` を登録する。
   Claude側にAPIキーを渡さないため、必ず自分のターミナルで実行すること：
   ```bash
   gh secret set ANTHROPIC_API_KEY
   ```
3. Vercel等のデプロイ先とGitHubリポジトリを連携する（push契機で自動デプロイされる）。

**ローカルでの試し方**

```bash
# .env.example を参考に、シェルでAPIキーをセットしてから実行する
ANTHROPIC_API_KEY=sk-ant-... npm run digest:collect
```

`scripts/collect-digest.ts` 内の `SYSTEM_PROMPT` は、既存の `data/digest.json` の
実例とサイトコピーから逆算した**初稿**。実際の掲載基準（何を「横丁」とみなすか等）は
Cowork側のmemoryが正のため、運用しながら文面を調整すること。

### 2. 手動更新（補助的な経路）

人手で内容を確定させたい場合のために、以下の経路も残している。

```bash
# 当日の新着記事一覧（DigestItem[]形式）をJSONファイルとして用意し、
npm run digest:update -- ./new-items.json
```

`DigestItem` の形式は `lib/types.ts` を参照してください。新着0件の日は
既存内容を維持する（Artifact版と同じルール）ようスクリプト側で担保しています。

### コンテンツ戦略の見直し（2026-09-05）

コピーライター3名・マーケティング戦略家・Webデザイナー・横丁コンサルタントに
よるレビューを経て、以下を実施済み：

- サービス名を「横丁メディア」→「ヨコチョナビ」に変更（上記参照）
- Heroコピーを「メディアの説明」から「読者の今夜の行動」を主語にした文へ変更し、
  Venuesセクションへ飛ぶCTAボタン（`.hero-cta`）を追加
- `collect-digest.ts` の `SYSTEM_PROMPT` から「〜動向を注視したい」等の内部メモ
  口調の指示を削除し、読者の検索意図に応える見出し・要約を書くよう変更
- SiteFooterに「特定の運営会社の広報物ではない」中立性の一文を追加

### 「読む・探す・つながる」実装ロードマップ（2026-09-05）

Webデザイナー・マーケティングコンサルタントによるレビューで、3本柱とも
コピー上の宣言のみで実体がないことが判明。優先度付けの結果、以下から着手：

1. **横丁詳細ページ（`/venues/[slug]`）— 実装済み**。Venuesカードの遷移先を
   外部サイト直リンクから内部詳細ページに変更し、検索流入をヨコチョナビ自身が
   受け止められるようにした（上記「写真について」参照）
2. **記事詳細ページ（`/articles/[slug]`）— 実装済み**（`data/articles.json`）
3. メール/LINE登録フォーム — 未着手（`ConnectSignup.tsx` はUIのみ先行実装、送信先は準備中表示）
4. Venuesのエリア/タグ簡易フィルタ — 未着手
5. 公式SNSアカウントのリンク設置 — 未着手（運営アカウント未開設のため保留）

### Heroの日本地図・ビジュアル刷新（2026-09-05）

- Hero写真の暗いオーバーレイと横線ノイズ（`.hero-photo::before`のグレイン演出）を
  削除し、写真そのものを前面に出す構成に変更。ワードマーク「ヨコチョナビ」は
  画面上部に小さく配置し、キャッチコピーは写真下部に移動
- 日本地図（`components/JapanMap.tsx`）のピンを、実際に横丁を試験掲載した
  13地域（札幌・仙台・東京・千葉・神奈川・名古屋・京都・大阪・岡山・広島・
  福岡・鹿児島・沖縄）に合わせて全面的に較正し直した。沖縄は元データ
  （Wikimedia Commonsの輪郭パス）に島の形状が含まれないため、日本地図の
  一般的な慣習に倣い、破線枠のインセット表示にしている
  （**旧バージョンの「広島」ピンは実際には大阪の東側＝紀伊半島付近に
  誤配置されていたため、今回西側の中国地方沿いに修正済み**）
- 地図の両サイドに、乾杯・団らんのイメージ写真を円形フレームで配置
  （`public/images/connect/circle-*.jpg`。未許諾の汎用イメージ、破線は
  「乾杯による縁のつながり」を表現する演出）
- Hero内の日本地図キャプション「全国各地に、横丁の灯りがある。」を
  見出しサイズに拡大
- 統計表示（本日の収集記事／掲載横丁数／最終更新）をカード形式に変更

### 全国横丁の試験掲載（2026-09-05）

浜倉的商店製作所グループ以外の12地域（東京を除く）についても、公開情報を
もとに代表的な横丁・飲食店街を1件ずつリサーチし、`data/venues.json` に
`status: "research"` として追加した（合計22横丁）。写真・ロゴは未掲載で、
カードから各運営者の公式サイト・出典ページへ外部リンクする形式。
Venuesセクション上部・カード内・フッターに「試験掲載（掲載許諾未取得）」
である旨を明記している。運営者への許諾取得・掲載可否確認は今後の実務課題
（上記「今後の検討事項」参照）。Venues見出しも社内向け文言
「プロトタイプ掲載対象の横丁」から、読者向けの「今夜、灯りをくぐれる横丁」
に変更した。

### 交流師プロフィール（2026-09-05）

トップページ下部（`components/UranoProfile.tsx`）に、横丁交流師・浦野雄次氏の
プロフィール欄を新設。写真は本人提供の3点から1点を使用（`public/images/urano-yuji.jpg`）。
コメント文は仮テキストであることを明記している。

### モバイル対応の不具合修正（2026-09-06）

ユーザーから「スマホで見ると本日の横丁ニュース以降が表示されない」との報告を受けて調査。
`components/Reveal.tsx`（スクロール時フェードイン演出）が使うIntersectionObserverが、
初回判定を取りこぼすことがあり、該当セクションが永久に`opacity:0`のまま表示されない
不具合を確認した（このBrowser環境でも再現）。原因の特定はできなかったが、症状として
致命的なため、**1秒後に要素が画面内(またはその手前400px以内)にあれば強制的に表示する
フォールバック**をReveal.tsxに追加し、恒久的な非表示を防止した。

併せて、Hero写真がスマホ縦画面では`background-size:cover`により極端にズーム
（アップ）されてしまう問題を修正。`max-width:640px`で`aspect-ratio:4/5`を
指定し、写真全体が視認できる比率に調整した（`.hero-photo`、globals.css）。

さらに、`npm run build`を`npm run dev`と同時に実行すると`.next`が競合し、
動的ルート（`/venues/[slug]`等）で500エラーが発生することが判明。
**本番ビルドと開発サーバーは同時に起動しないこと**を運用上の注意点とする。

### 日本地図ピンの再較正（2026-09-06）

ユーザー提供の都道府県参考図（一般的な日本地図の構図。北海道は左上・沖縄は
右下にインセット表示される配置）をもとに、Heroの日本地図（`components/JapanMap.tsx`）
の各ピン位置を再検証。特に**鹿児島のピンが福岡から西へ離れすぎていた点**
（本来は福岡からほぼ真南、やや西程度のはず）と、**大阪・京都の位置関係**
（京都はやや東寄りにすべきところ、大阪より東になっていた点）を修正。
関西・中部圏のピン間隔も、密集しすぎて判別しづらかった点を少し広げて調整した。

### Heroの日本地図バンドをリッチ化（2026-09-06）

ユーザーから提供されたデザインモックアップ（写真パネル+キャプション+リード文構成）を
参考に、Hero下部「全国各地に、横丁の灯りがある。」セクションを拡張。

- 見出し下に短いリード文「路地の先に、人がいて、味があって、物語がある。
  日本のどこかで、きっとまた。」を追加
- 地図の両サイドの円形サムネイル（`circle-*.jpg`、装飾のみ）を、縦長の
  大きめ写真パネル2枚（`public/images/connect/panel-left.jpg` /
  `panel-right.jpg`）に置き換え、各パネルに短いコピーを重ねた
  （「あの街の、あの横丁で。また、乾杯しよう。」「知らない街で、
  好きな横丁ができる。」）。モバイルでは地図の上下に縦積みで表示される
- 地図バンド下部に横長の写真バナー（`panel-bottom.jpg`）とコピー
  「横丁は、いつも旅のそばにある。」を追加
- いずれも未許諾の汎用イメージカット（既存の`hero-cover.jpg`等と同素材）で、
  装飾目的のため個別の「イメージ」タグは付けていない（`alt=""`で処理）

### 全国横丁ディレクトリ（旅×横丁）の追加（2026-09-06）

ユーザーから、外部AI（ChatGPT）による「全国横丁基盤データ調査レポート」
（76件の横丁・信頼性ランク付き・出典URL付き）の提供を受け、これをもとに
`/travel`ページ（`app/travel/page.tsx`）を新設した。

- データは `data/yokocho-directory.json` に地域別（北海道・東北／関東／中部／
  関西・中国・四国／九州・沖縄）で格納。既存の掲載横丁（`venues.json`）と
  重複する横丁（恵比寿横丁・渋谷横丁・有楽町産直横丁・新橋ガード下横丁・
  横浜ハマ横丁・龍乃都飲食街・新宿カブキHALL・野毛たべもの横丁・先斗町・
  中洲屋台横丁・天文館かごしま横丁）は重複掲載を避けるため除外している
- `venues.json`（掲載許諾済み＋試験掲載の厳選22横丁、写真あり）とは別の、
  「写真なし・出典リンクのみ」の全国網羅型ディレクトリという位置づけ。
  両者の役割の違いをコード上のコメント・サイト上の文言双方で明記している
- Pillars「探す・行く」カードのリンク先を `/#venues`（厳選横丁カード）から
  `/travel`（全国ディレクトリ）に変更。Venuesセクション末尾にも
  `/travel` への導線を追加した
- 本データはAIによる一次調査であり、店舗数・営業状況は変動するため、
  各カードから出典・公式ページへの直接確認を促す文言をページ内に明記している

### 「探す・比較する・決める」機能強化（2026-09-06）

ユーザーから「利用者目線で、探す・比較する・決めるを強化したい」との7点の
機能要望を受け、Venuesセクションと横丁詳細ページを拡張した。

- **現在地から探す**：Hero下部のCTAを1個から3個（「今夜行ける横丁を探す」
  「📍現在地から探す」「目的から探す」）の横並びボタンに変更。「現在地から
  探す」は `/?geo=1#venues` に遷移し、Venuesセクション側で自動的に
  `navigator.geolocation` を起動する導線にした（新規 `components/VenueExplorer.tsx`）
- **目的別タグ**：「一人飲み」「デート」「友人」「観光」「昭和レトロ」
  「地酒」「昼飲み」等のタグをチップUIで一覧上部に表示し、クリックで絞り込み
- **共通スペック表示**：一人飲み適性・初心者向け・交流度・にぎやかさ・
  ローカル感を★5段階、価格帯・滞在目安をテキストで表示（カード上は簡易表示、
  詳細ページは全項目のグリッド表示）
- **交流度**：ヨコチョナビ独自の差別化指標として★5段階スターUIを新設
  （`StarRating`コンポーネント、`components/VenueExplorer.tsx`からexport）
- **編集部おすすめコメント**：各横丁に一言コメントを追加し、カード・詳細
  ページ両方に表示
- **営業時間バナー**：詳細ページ上部に営業時間バナーを新設。ただし
  **実際の営業時間データはまだ取得できていないため、現時点では全横丁で
  「最新情報は公式サイトでご確認ください」という誠実なフォールバック表示**
  にしている（`hours`フィールドはデータ構造として用意済み、値が入り次第
  実際の営業時間表示に自動的に切り替わる）
- **TOPコピー連動**：上記の3ボタンCTAで「今夜行ける横丁を探す」
  「現在地から探す」「目的から探す」をコピーとして直接機能に接続した

**スコープ上の注意（意図的な制限）**：タグ・スペック・編集部コメント・
緯度経度は、エリア・写真が確認済みの掲載許諾済み横丁8件
（恵比寿・渋谷・大宮・有楽町・龍乃都・カブキHALL・新橋・アメ横）のみに
付与した。エリア未確定の「立ち寿司横丁」「ハマ横丁」、および運営元確認前の
「ピット・イン」、全国ディレクトリの「試験掲載」11件には、根拠のない
評価値を作らないという方針のもと、これらのフィールドを付与していない。
今後、現地取材・許諾取得が進み次第、対象を順次拡大する想定。

### 改善指示書に基づくトップページ刷新（2026-09-06）

ユーザーから「ClaudeCode_ヨコチョナビ改善指示書.docx」（全20章）の提供を受け、指示書自身の
実行手順（STEP4「優先度Aのみ実装する」）に従い、最優先の5項目（A1ファーストビュー／
A2横丁検索導線／A3 MAP／A4横丁カード／A5スマホUX）のみを今回実装した。YOKOCHO PEOPLE・
NEO YOKOCHOブランド化・インバウンド導線・徒歩導線・事業者向けセクション・全体の文章削減・
角丸/グラデーション/影の全面的なデザイン言語見直しは、指示書自身の方針により今回は対象外
（将来ラウンドで着手）。

- **A1 ファーストビュー**：メインコピーを「今夜、知らない横丁へ。」に短縮し、サブコピー
  「全国の横丁を、地図から、人から、気分から。あなたの「今夜」を見つける横丁ガイド。」を追加。
  CTAを2ボタン（「横丁を探す」「📍現在地から探す」）に整理し、旧来の地図バンド（装飾パネル・
  バナー・統計）はHeroから切り出してA3セクションへ移設、Hero自体は写真+コピー+CTAのみの
  シンプルな構成にした。
- **A2 横丁検索導線**：新規セクション`components/MoodSearch.tsx`（「今夜どんな気分?」）を
  Hero直後に追加。実データに存在するタグをチップ表示し、クリックで
  `?tag=<値>#venues`へ遷移、`VenueExplorer`側で自動的に絞り込む（既存の`?geo=1`パターンを
  踏襲して拡張）。
- **A3 MAP**：新規セクション`components/MapSection.tsx`を追加し、既存の静的イラスト地図の上に
  5地方ブロック（北海道・東北／関東／中部／関西・中国・四国／九州・沖縄）の大まかなクリック
  領域を重ねた。関東ブロックは`#venues`へ、他地方は`/travel`の該当地域アンカーへ遷移する
  （掲載許諾済み横丁は現状すべて関東のため）。ピン単位の座標較正は行わず、地方ブロック単位の
  大まかな範囲に留めることで、以前繰り返し発生した座標ズレの問題を再発させない設計とした。
  `/travel`側には`lib/regions.ts`で定義した地域名↔ASCIIアンカーIDの対応表を用いて`id`属性を
  追加している。
- **A4 横丁カード**：指示書18章は「星評価NG」としているが、前回実装済みの「交流度」★5段階は
  ユーザー判断により維持。一方でカード上の価格帯・滞在目安の同時表示はやめ、価格帯のみに簡素化
  （滞在目安は詳細ページのみで表示）し、★評価・タグ・編集部コメントを主役にした。指示書が
  求める「店舗数・規模感」は検証済みデータが無いため追加していない（根拠のない数値を作らない
  という本プロジェクトの一貫方針）。
- **A5 スマホUX**：`.tag-chip`・`.locate-button`・新設の地図クリック領域・`.mood-tag`等の
  タップ対象を最低40〜44px相当の高さに統一した。
- **タグの追加**：指示書が挙げる「初心者歓迎」「せんべろ」「地元民御用達」「NEO横丁」を、
  `specs.beginner>=4`（初心者歓迎）、`specs.price==="¥"`かつ`specs.local>=4`
  （せんべろ・地元民御用達）、既存の編集部コメントが多層階エンタメ空間である旨を明記している
  venue（NEO横丁：龍乃都飲食街・新宿カブキHALL）という客観的な根拠がある場合のみ追加した。
  演出目的での主観的なタグ付けは行っていない。
- **ホームページの並び替え**：`Hero → MoodSearch(新規) → MapSection(新規) → Venues →
  Pillars → Digest → UranoProfile → SiteFooter`に変更し、各セクションの`section-num`表記
  （01〜06）も並びに合わせて更新した。

### 実機レビューを踏まえた微調整（2026-09-06）

上記のトップページ刷新をユーザーがローカルで確認した際のフィードバックを反映。

- 「本日の横丁ニュース」（Digest）が並び替え後に下の方に埋もれ、「新着ニュースは早めに知りたい」
  とのフィードバックを受け、Hero直後（01/Digest）へ再度昇格。Mood検索・MAP・Venuesの
  「探す」導線はそのままHero直後にひとまとまりで続く構成を維持した
  （`Hero → Digest → MoodSearch → MapSection → Venues → Pillars → UranoProfile`）。
- ユーザーから追加された横丁「グランハマー」（新橋SL広場前、株式会社浜倉的商店製作所運営、
  縦型横丁業態）を`data/venues.json`の先頭に追加し、横丁一覧の最上部に表示されるようにした。
  当初は写真・ロゴの掲載許諾が未確認のため`status: "research"`（試験掲載）としていたが、
  ユーザーより「浜倉的商店製作所なので写真公開で大丈夫」と確認が取れたため、公式サイト
  （grandhammer.jp）からロゴ（`public/images/venues/grandhammer.jpg`）とキービジュアル
  （タイ屋台フロア「Yokocho Asia」の様子、`public/images/venues/photo-grandhammer.jpg`）を
  取得し、他の掲載許諾済み横丁と同様の`status: "licensed"`・`slug: "grandhammer"`に格上げ
  （詳細ページ `/venues/grandhammer` も生成される）。タグ・スペック・編集部コメントは
  現地取材前のため、他の`licensed`横丁のうち`tachizushi`/`hama`と同様、まだ付与していない。

### 今後の検討事項（未実装・要設計判断）

- `collect-digest.ts` の `SYSTEM_PROMPT`（掲載基準の初稿）を、Cowork側のmemoryにある
  実際の掲載基準と突き合わせて精緻化する
- MAP検索・店舗ページなど、まだArtifact版にもない機能の設計
- 浜倉的商店製作所グループとの掲載許諾・データ提供の実務フロー
- 試験掲載中12横丁（浜倉グループ以外）への掲載許諾取得・写真提供依頼
- ピット・インの運営状況の確認（現行ブランドか、掲載継続の是非）
- 浦野雄次氏プロフィールのコメント文（現在は仮テキスト）の本文確定

## 背景（企画メモ）

企画の背景・収益モデル・KPI等は、この場では管理していません（Cowork側のmemoryで
浦野雄次さんと継続管理しています）。実装上の意思決定で企画意図の確認が必要な場合は、
Cowork側に立ち戻って確認してください。
